import { asset } from '$app/paths';

/** m4a masters in `static/atelier/`; Opus/WebM siblings from `encode-audio`. */
export const NEEDLE_DROP_SRC = asset('/atelier/needle-drop.m4a');
export const NEEDLE_LIFT_SRC = asset('/atelier/needle-lift.m4a');
